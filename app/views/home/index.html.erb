
<h1 style="text-align:center;">친구 만나느라 샤샤샤</h1>

<div class = "container" style="margin-top:30px;">
    <form style="margin-top:40px;" action="/home/write" method="POST">
  
      <div class="form-group">
        <label for="exampleInputPassword1">제목</label>
        <input type="text" name= "title" class="form-control" id="exampleInputPassword1" placeholder="Title">
      </div>
      <label for="exampleInputPassword1">내용</label>
        <textarea class="form-control" rows="5" id="content" name="content" placeholder="Content"></textarea>
        <button type="submit" class="btn btn-default">Submit</button>
    </form>

 <% @posts.each do |p| %>
  <div class="well">
   <span style="font-weight:bold;"><%= p.title %></span><br>
  <%= p.content %>
    <p>모든 글자 수: <%=p.content.length-1%></p> 

    <p>띄어쓰기 제외 글자 수: <%=p.content.length - p.content.count(' ')%> </p> 

    <p>단어 갯 수: <%=p.content.split(' ').length%></p> 
  <% p.replies.each do |r| %>
   <p><%= r.content %></p>
  <% end %>
 
 
  <div>
    <form style="margin-top:40px;" action="/home/reply_write" method="POST">
      <input type= "hidden" value="<%= p.id %>" name="id_of_post">
      <textarea class="form-control" rows="5" id="content" name="content"></textarea>
      <button type="submit" class="btn btn-default">Submit</button>
    </form>
  </div>
</div> 
 <% end %>

